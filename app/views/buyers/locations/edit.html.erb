<% content_for(:page_header) { 'District Profile' } %>

<%= form_with model: [:buyers, @location], url: buyers_district_profile_location_path(@location), method: :patch, local: true do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>
