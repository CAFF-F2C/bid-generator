<% content_for(:page_header) { 'District Profile' } %>
<%= render(Buyers::DistrictProfileNavComponent.new(current_path: request.path)) %>
<%= render(FormErrorsComponent.new(errors: @district_profile.errors)) %>
<%= form_with model: [:buyers, @district_profile], url: buyers_district_profile_url, method: :patch, local: true, data: {turbo: false} do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>
