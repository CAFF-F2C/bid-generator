<% content_for(:page_header) { 'My Documents' } %>

<%= render 'profile_complete_prompt' unless @current_buyer.district_profile&.complete? %>
<%= render 'documents_list' if @documents.present? %>
<%= render 'documents_empty' unless @documents.present? %>
