<% content_for(:page_header) { "Request for proposal - #{@rfp.name}" } %>
<%= render(Buyers::RfpNavComponent.new(current_path: request.path, current_rfp: @rfp)) %>
<%= form_with model: [:buyers, @rfp], url: buyers_rfp_path(@rfp), method: :patch, local: true, data: {turbo: false} do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>
